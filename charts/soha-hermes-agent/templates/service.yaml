apiVersion: v1
kind: Service
metadata:
  name: {{ include "soha-hermes-agent.fullname" . }}
  labels:
    {{- include "soha-hermes-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "soha-hermes-agent.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
